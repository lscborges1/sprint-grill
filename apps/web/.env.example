# Personal Access Token do Azure DevOps (escopos de leitura e escrita de work items).
# Segredo: nunca vai para o arquivo de config nem para o log.
AZURE_DEVOPS_PAT=

# trace | debug | info | warn | error | fatal — padrão: info
LOG_LEVEL=info
